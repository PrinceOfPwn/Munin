# Context7 API Key for MCP server (https://context7.com)
# Copy this to your local .env file (not committed).
CONTEXT7_API_KEY=ctx7sk-REPLACE_ME

# GitHub token (already provided by gh CLI auth / GitHub Actions secrets)
GITHUB_TOKEN=ghp_your_token_here
